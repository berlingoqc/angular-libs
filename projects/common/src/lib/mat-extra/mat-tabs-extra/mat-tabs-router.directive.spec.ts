import { MatTabsRouterDirective } from './mat-tabs-router.directive';

describe('MatTabsRouterDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTabsRouterDirective();
    expect(directive).toBeTruthy();
  });
});
