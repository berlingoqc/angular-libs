// PropertyComponent est une composante pour afficher les données
// d'une propriétés, elle supporte un ensemble de sous-type
export interface PropertyComponent {
  // Nom de la propertyComponent
  name: string;
}
